---
import { nav } from '@/data/site';
import { projects } from '@/data/projects';

// Build a flat list of jump targets: nav sections, top projects, resume, blog.
const navItems = nav.map((item) => ({
  label: item.label,
  sub: 'Section',
  href: item.href,
  keywords: item.label,
}));

const projectItems = projects.slice(0, 6).map((p) => ({
  label: p.title,
  sub: 'Project',
  href: p.href,
  keywords: `${p.title} ${p.tags.join(' ')}`,
}));

const staticItems = [
  { label: 'Resume', sub: 'Page', href: '/resume', keywords: 'resume cv' },
  { label: 'Blog — all posts', sub: 'Page', href: '/blog', keywords: 'blog writing posts' },
];

const allItems = [...navItems, ...staticItems, ...projectItems];
---

<!-- The trigger button lives in Header.astro; this is just the overlay. -->
<div class="cp-overlay" data-command-palette role="dialog" aria-modal="true" aria-label="Quick navigation">
  <div class="cp-panel">
    <div class="cp-input-row">
      <span class="cp-input-icon" aria-hidden="true">→</span>
      <input
        type="text"
        data-cp-input
        class="cp-input"
        placeholder="Jump to a section, project, or page…"
        autocomplete="off"
        spellcheck="false"
      />
      <kbd class="cp-esc">Esc</kbd>
    </div>

    <ul class="cp-results" data-cp-results>
      {allItems.map((item) => (
        <li
          class="cp-item"
          data-cp-item
          data-cp-href={item.href}
          data-cp-keywords={item.keywords}
          tabindex="-1"
        >
          <span class="cp-item__label">{item.label}</span>
          <span class="cp-item__sub">{item.sub}</span>
        </li>
      ))}
    </ul>
  </div>
</div>

<script>
  import { initCommandPalette } from '@/scripts/commandPalette';
  initCommandPalette();
</script>

<style>
  .cp-overlay {
    position: fixed;
    inset: 0;
    z-index: 100;
    display: flex;
    align-items: flex-start;
    justify-content: center;
    padding-top: min(14vh, 6rem);
    background: rgba(33, 53, 61, 0.55);
    backdrop-filter: blur(4px);
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.18s ease;
  }

  .cp-overlay.cp-open {
    opacity: 1;
    pointer-events: auto;
  }

  .cp-panel {
    width: min(34rem, 90vw);
    background: var(--paper-raised);
    border: 1px solid var(--line-strong);
    border-radius: var(--radius-lg);
    box-shadow: 0 24px 60px -12px rgba(33, 53, 61, 0.45);
    overflow: hidden;
    transform: translateY(-8px) scale(0.98);
    transition: transform 0.18s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .cp-overlay.cp-open .cp-panel {
    transform: translateY(0) scale(1);
  }

  .cp-input-row {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 1.25rem;
    border-bottom: 1px solid var(--line);
  }

  .cp-input-icon {
    color: var(--teal);
    font-family: var(--font-mono);
  }

  .cp-input {
    flex: 1;
    background: none;
    border: none;
    outline: none;
    color: var(--fg-primary);
    font-family: var(--font-body);
    font-size: 1rem;
  }

  .cp-input::placeholder {
    color: var(--fg-faint);
  }

  .cp-esc {
    padding: 0.15em 0.5em;
    border: 1px solid var(--line-strong);
    border-radius: 5px;
    font-size: 0.7rem;
    color: var(--fg-faint);
    font-family: var(--font-mono);
  }

  .cp-results {
    max-height: 60vh;
    overflow-y: auto;
    padding: 0.5rem;
  }

  .cp-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.7rem 0.85rem;
    border-radius: var(--radius-sm);
    cursor: pointer;
    color: var(--fg-muted);
    transition: background-color 0.12s ease, color 0.12s ease;
  }

  .cp-item__label {
    color: var(--fg-primary);
    font-size: 0.95rem;
  }

  .cp-item__sub {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--fg-faint);
  }

  .cp-item--active {
    background: color-mix(in srgb, var(--coral) 30%, transparent);
  }

  .cp-item--active .cp-item__label {
    color: var(--ink);
  }
</style>
